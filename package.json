{
  "name": "ai-agent-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"python -m uvicorn app.main:app --reload --app-dir backend\" \"npm --prefix exam-app run dev\""
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "concurrently": "^9.0.0"
  }
}
