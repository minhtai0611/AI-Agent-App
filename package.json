{
  "name": "ai-agent-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"PYTHONPATH=backend uvicorn app.main:app --reload\" \"npm --prefix exam-app run dev\""
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
