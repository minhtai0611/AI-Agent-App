fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic-settings>=2.2.0
python-dotenv>=1.0.0
httpx>=0.27.0
pytest>=8.0.0
pytest-asyncio>=0.23.0
aiosqlite>=0.20
sympy>=1.12
workos>=5.0.0
jinja2>=3.1.0
# Needs system Cairo/Pango libraries (not a pure-pip install) — see
# backend/app/org_reports.py, which lazy-imports this and 503s if unavailable.
weasyprint>=62.0
